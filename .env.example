# Optional: set these only when you run your own peerjs-server on LAN.
# If left unset, PeerJS will use its default public signaling server.
VITE_PEER_HOST=192.168.1.10
VITE_PEER_PORT=9000
VITE_PEER_PATH=/
VITE_PEER_SECURE=false
